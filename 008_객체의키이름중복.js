// 문제
var d = {
    height: 180,
    weight: 78,
    weight: 84,
    temperature: 36,
    eyesight: 1,
};

console.log(d['weight']); // 84
console.log(d.weight); // 84

// 객체의 키는 숫자 또는 문자열만 가능함. 문자열에 따옴표 생략 가능.
// 객체의 키 이름이 중복되면, 마지막 키의 값을 가져옴
